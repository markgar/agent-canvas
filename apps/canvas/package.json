{
  "name": "@agent-canvas/canvas",
  "version": "0.1.0",
  "private": true,
  "description": "A local, MCP-driven visual surface for AI assistants.",
  "license": "UNLICENSED",
  "type": "module",
  "engines": {
    "node": ">=22.14.0 <23",
    "npm": ">=10"
  },
  "scripts": {
    "dev": "tsx watch src/server/main.ts",
    "build": "node scripts/build.mjs",
    "start": "node dist/server/main.js",
    "typecheck": "tsc --noEmit && tsc --project tsconfig.server.json && tsc --project tsconfig.client.json && tsc --project tsconfig.contracts.json"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "css-tree": "3.2.1",
    "fastify": "^5.12.3",
    "sanitize-html": "2.17.7",
    "zod": "^4.0.0"
  }
}
